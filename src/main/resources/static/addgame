let playerRoster = document.getElementById("playerList").getElementsByTagName("option").value;

function createTeam(tm, playerRoster) {
	let selectablePlayers = playerRoster;
	for (let i=0; i < selectablePlayers.length; i++) {
	    let player = selectablePlayers[i];
	    
	    if (player.selected) {
	    	result = playerRoster.options[playerList.selectedIndex].value;
            tm.push(result);
            console.log(tm);
	    	}
        }
	const url3 = 'http://localhost:8081/gameplan/{gameId}'
	axios.put(url3, tm)
	.then((response) => {
        addTeamToPlan();
        console.log(response);})
    .catch(e => {
        console.log(e);
    });
	
}

function getPlayerById(playerId){ 
    let url3 = 'http://localhost:8081/player/{id}';
    
    playerId = document.getElementById(playerRoster.option.value);

    return axios.get(url, playerId
        ).then((response) => {
            console.log(response);
        }).catch(e => {
            console.log(e);
        });
    }

/*
function addTeamToPlan() {
	const url3 = 'http://localhost:8081/gameplan/{gameId}'
	axios.put(url3)
    .then((response) => {
        addTeamToPlan();
        console.log(response);})
    .catch(e => {
        console.log(e);
    });
	
}

*/

    



    
    